# Mini-OFBiz 实体引擎任务拆解清单

> 本文档将原15天任务拆解为更细粒度的子任务，每个子任务可独立执行、独立验收。
> 状态说明：⬜ 待开始 | 🔄 进行中 | ✅ 已完成 | ⏸️ 已阻塞

---

## 目录

- [阶段一：项目骨架搭建](#阶段一项目骨架搭建)
- [阶段二：元数据核心开发](#阶段二元数据核心开发)
- [阶段三：实体引擎核心](#阶段三实体引擎核心)
- [阶段四：CRUD功能实现](#阶段四crud功能实现)
- [阶段五：企业级特性](#阶段五企业级特性)
- [阶段六：RBAC权限系统](#阶段六rbac权限系统)

---

## 阶段一：项目骨架搭建

### T1.1 父工程POM配置
**状态：** ⬜ 待开始

**详细设计：**
创建Maven父工程 `mini-ofbiz-entity`，统一管理所有子模块依赖版本。采用 `dependencyManagement` 和 `pluginManagement` 集中声明版本号，确保多模块依赖一致性。

**代码编写要点：**
- 父POM配置JDK21编译版本
- 声明SpringBoot 4.0.6 BOM依赖
- 定义公共属性：`<java.version>21</java.version>`、`<spring-boot.version>4.0.6</spring-boot.version>`
- 配置Maven编译插件支持JDK21
- 预留四个子模块声明：core、autoconfig、starter、demo

**自测验收：**
- [ ] 执行 `mvn clean compile` 无报错
- [ ] 检查依赖版本树正确：`mvn dependency:tree`
- [ ] IDE正确识别JDK21

**依赖：** 无

---

### T1.2 Core模块骨架创建
**状态：** ⬜ 待开始

**详细设计：**
创建 `mini-ofbiz-entity-core` 模块，定义核心包结构。此模块为零业务依赖的纯Java模块，仅依赖基础工具库（dom4j、slf4j等）。

**代码编写要点：**
- 创建包结构：
  ```
  com.mini.ofbiz.core
  ├── parser      # XML解析
  ├── model       # 元数据模型
  ├── entity      # 充血实体
  ├── delegator   # 操作引擎
  ├── sql         # SQL构建
  ├── cache       # 缓存
  ├── transaction # 事务
  ├── rbac        # 权限核心
  └── exception   # 异常定义
  ```
- 编写模块pom.xml，引入dom4j、slf4j-api
- 创建基础异常类：`EntityException`、`EntityNotFoundException`

**自测验收：**
- [ ] 模块可独立编译
- [ ] 包结构创建完整
- [ ] 无编译错误

**依赖：** T1.1

---

### T1.3 Autoconfig模块骨架创建
**状态：** ⬜ 待开始

**详细设计：**
创建 `mini-ofbiz-entity-autoconfig` 模块，负责SpringBoot自动配置逻辑。依赖SpringBoot4自动配置starter和core模块。

**代码编写要点：**
- 引入依赖：`spring-boot-autoconfigure`、`mini-ofbiz-entity-core`
- 创建包结构：
  ```
  com.mini.ofbiz.autoconfig
  ├── properties   # 配置属性类
  └── configuration # 自动配置类
  ```
- 创建空配置类 `MiniOfbizAutoConfiguration` 占位
- 创建 `META-INF/spring` 目录（SpringBoot4新规范）

**自测验收：**
- [ ] 模块可独立编译
- [ ] 依赖core模块正确
- [ ] SpringBoot自动配置目录存在

**依赖：** T1.2

---

### T1.4 Starter模块创建
**状态：** ⬜ 待开始

**详细设计：**
创建 `mini-ofbiz-entity-starter` 模块，作为用户引入的唯一依赖。Starter仅做依赖聚合，不包含任何代码。

**代码编写要点：**
- pom.xml仅引入：`mini-ofbiz-entity-autoconfig`
- 设置打包方式为 `jar`
- 无需任何Java代码
- 添加README说明使用方式

**自测验收：**
- [ ] 模块编译通过
- [ ] 依赖传递正确

**依赖：** T1.3

---

### T1.5 Demo模块创建与启动验证
**状态：** ⬜ 待开始

**详细设计：**
创建 `mini-ofbiz-entity-demo` 模块，作为测试演示工程。引入starter依赖、MySQL驱动、SpringBoot Web启动器。

**代码编写要点：**
- 引入依赖：`mini-ofbiz-entity-starter`、`spring-boot-starter-web`、`mysql-connector-j`
- 创建主启动类 `DemoApplication`
- 创建 `application.yml` 配置数据源
- 编写HelloController测试接口

**自测验收：**
- [ ] 启动类可正常运行
- [ ] 访问 `/hello` 返回正常
- [ ] 控制台无ERROR日志

**依赖：** T1.4

---

## 阶段二：元数据核心开发

### T2.1 ModelEntity实体元数据类
**状态：** ⬜ 待开始

**详细设计：**
`ModelEntity` 是实体元数据的核心容器，封装一个XML实体定义的全部信息。包含实体名、表名、字段列表、主键列表、索引列表等。

**代码编写要点：**
```java
public class ModelEntity {
    private String entityName;      // 实体名 如 "User"
    private String tableName;        // 表名 如 "sys_user"
    private String packageName;      // 包名
    private List<ModelField> fields; // 字段列表
    private List<ModelField> pkFields; // 主键字段列表
    private List<ModelIndex> indexes; // 索引列表
    private String description;      // 描述
    
    // 工具方法
    public ModelField(String fieldName);
    public String getPkFieldName();
    public int getPkSize();
}
```

**自测验收：**
- [ ] 可正确设置/获取所有属性
- [ ] `has()` 方法正确判断字段存在
- [ ] 主键字段获取正确

**依赖：** T1.5

---

### T2.2 ModelField字段元数据类
**状态：** ⬜ 待开始

**详细设计：**
`ModelField` 描述单个字段的完整元数据，包括字段名、列名、语义类型、是否主键、是否非空、默认值等。

**代码编写要点：**
```java
public class ModelField {
    private String name;           // 字段名（Java风格）
    private String colName;         // 列名（数据库风格）
    private String type;            // 语义类型
    private boolean isPk;           // 是否主键
    private boolean notNull;        // 是否非空
    private String defaultValue;    // 默认值
    private int length;             // 长度
    private String description;     // 描述

    // 是否自增主键
    private boolean isAutoGenerated;
}
```

**自测验收：**
- [ ] 所有属性可正确设置/获取
- [ ] 提供链式setter

**依赖：** T2.1

---

### T2.3 ModelIndex索引元数据类
**状态：** ⬜ 待开始

**详细设计：**
`ModelIndex` 描述数据库索引结构，支持普通索引和唯一索引。

**代码编写要点：**
```java
public class ModelIndex {
    private String name;           // 索引名
    private List<String> fieldNames; // 索引字段列表
    private boolean unique;        // 是否唯一索引
}
```

**自测验收：**
- [ ] 可正确描述单字段/复合索引
- [ ] unique属性正确

**依赖：** T2.1

---

### T2.4 FieldType语义类型枚举
**状态：** ⬜ 待开始

**详细设计：**
定义OFBiz标准语义类型枚举，用于声明字段的数据语义。语义类型独立于具体数据库，由引擎自动映射到数据库类型。

**代码编写要点：**
```java
public enum FieldType {
    ID("id", "VARCHAR(20)"),           // 主键ID
    ID_NE("id-ne", "VARCHAR(20)"),      // 非空主键
    NAME("name", "VARCHAR(100)"),       // 名称
    DESCRIPTION("description", "VARCHAR(255)"), // 描述
    VALUE("value", "DECIMAL(18,2)"),    // 金额数值
    
    DATE("date", "DATE"),               // 日期
    DATE_TIME("date-time", "DATETIME"), // 日期时间
    TIME("time", "TIME"),               // 时间
    
    TEXT("text", "TEXT"),               // 长文本
    VERY_LONG("very-long", "VARCHAR(2000)"), // 长字符串
    
    INDICATOR("indicator", "CHAR(1)"),   // 布尔标识 Y/N
    INTEGER("integer", "INT"),          // 整数
    LONG("long", "BIGINT"),             // 长整数
    FLOAT("float", "DOUBLE"),           // 浮点数
    
    BLOB("blob", "LONGBLOB");           // 二进制

    private final String semantic;
    private final String defaultSqlType;
    
    // 获取映射的SQL类型
    public String getSqlType(Integer length);
}
```

**自测验收：**
- [ ] 所有语义类型定义完整
- [ ] `getSqlType()` 返回正确的MySQL类型
- [ ] 支持长度参数自定义

**依赖：** T2.2

---

### T2.5 EntityModelBuilder元数据构建器
**状态：** ⬜ 待开始

**详细设计：**
提供流式API构建ModelEntity，方便单元测试和代码中动态创建实体模型。

**代码编写要点：**
```java
public class EntityModelBuilder {
    private ModelEntity entity;
    
    public static EntityModelBuilder create(String entityName, String tableName);
    public EntityModelBuilder field(String name, String type);
    public EntityModelBuilder pkField(String name);
    public EntityModelBuilder field(String name, String type, int length);
    public EntityModelBuilder notNull();
    public EntityModelBuilder defaultValue(String value);
    public EntityModelBuilder index(String... fieldNames);
    public EntityModelBuilder uniqueIndex(String... fieldNames);
    public ModelEntity build();
}
```

**自测验收：**
- [ ] 可流式构建实体模型
- [ ] 生成的ModelEntity结构正确
- [ ] 编写单元测试验证

**依赖：** T2.3, T2.4

---

### T2.6 XML结构设计与示例文件
**状态：** ⬜ 待开始

**详细设计：**
设计 `entitymodel.xml` 的XML Schema，兼容OFBiz基础格式。创建示例实体定义文件供解析测试。

**代码编写要点：**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<entitymodel>
    <entity entity-name="TestUser" table-name="test_user" package="test">
        <field name="userId" type="id-ne" is-pk="true"/>
        <field name="username" type="name"/>
        <field name="email" type="description"/>
        <field name="status" type="indicator" default="A"/>
        <field name="createTime" type="date-time"/>
        <index name="IDX_USERNAME" unique" unique="true">
            <index-field name="username"/>
        </index>
    </entity>
</entitymodel>
```

**自测验收：**
- [ ] XML文件格式正确
- [ ] 放置于 `resources/entitydef/` 目录

**依赖：** T2.5

---

### T2.7 XmlEntityParser核心解析器
**状态：** ⬜ 待开始

**详细设计：**
实现基于Dom4j的XML解析器，将entitymodel.xml解析为ModelEntity对象列表。

**代码编写要点：**
```java
public class XmlEntityParser {
    
    /**
     * 解析单个XML文件，返回实体列表
     */
    public List<ModelEntity> parse(InputStream inputStream) throws EntityException;
    
    /**
     * 解析指定路径下所有entitymodel.xml
     */
    public List<ModelEntity> parseAll(String... locations) throws EntityException;
    
    // 内部解析方法
    private ModelEntity parseEntity(Element entityElement);
    private ModelField parseField(Element fieldElement);
    private ModelIndex parseIndex(Element indexElement);
}
```

**自测验收：**
- [ ] 可正确解析示例XML文件
- [ ] 解析结果字段数、主键、索引全部正确
- [ ] 异常情况正确抛出EntityException

**依赖：** T2.6

---

### T2.8 ModelReader全局元数据读取器
**状态：** ⬜ 待开始

**详细设计：**
`ModelReader` 是元数据的全局访问入口，负责加载、缓存、查询所有实体模型。采用线程安全的ConcurrentHashMap存储。

**代码编写要点：**
```java
public class ModelReader {
    private final Map<String, ModelEntity> entityCache = new ConcurrentHashMap<>();
    private final XmlEntityParser parser;
    
    /**
     * 初始化：加载所有实体模型
     */
    public void initialize(String... locations);
    
    /**
     * 根据实体名获取模型
     */
    public ModelEntity getModelEntity(String entityName);
    
    /**
     * 检查实体是否存在
     */
    public boolean exists(String entityName);
    
    /**
     * 获取所有实体名
     */
    public Set<String> getAllEntityNames();
    
    /**
     * 清空缓存
     */
    public void clear();
}
```

**自测验收：**
- [ ] 可加载多个XML文件
- [ ] `getModelEntity()` 返回正确模型
- [ ] 线程安全测试通过

**依赖：** T2.7

---

## 阶段三：实体引擎核心

### T3.1 GenericValue充血实体设计
**状态：** ⬜ 待开始

**详细设计：**
`GenericValue` 是OFBiz核心充血实体，基于Map结构存储字段数据。封装CRUD操作方法，绑定Delegator和ModelEntity。

**代码编写要点：**
```java
public class GenericValue implements Map<String, Object> {
    private String entityName;
    private ModelEntity modelEntity;
    private Delegator delegator;
    private Map<String, Object> fields = new LinkedHashMap<>();
    
    // ================== 字段访问 ==================
    
    @Override
    public Object get(Object key);
    
    @Override
    public Object put(String key, Object value);
    
    // ================== 类型安全访问 ==================
    
    public String getString(String name);
    public Integer getInteger(String name);
    public Long getLong(String name);
    public BigDecimal getBigDecimal(String name);
    public LocalDateTime getLocalDateTime(String name);
    public <T> T getValue(String name, Class<T> type);
    
    // ================== 主键操作 ==================
    
    public Object getPrimaryKey();
    public void setPrimaryKey(Object value);
    
    // ================== 充血方法 ==================
    
    public void create() throws EntityException;
    public void store() throws EntityException;
    public void remove() throws EntityException;
    
    // ================== 校验方法 ==================
    
    public void validateFields() throws EntityException;
}
```

**自测验收：**
- [ ] 可正确存储/获取字段值
- [ ] 类型安全方法返回正确类型
- [ ] Map接口方法全部实现

**依赖：** T2.8

---

### T3.2 GenericValueFactory实体工厂
**状态：** ⬜ 待开始

**详细设计：**
工厂类负责创建GenericValue实例，绑定元数据和Delegator。

**代码编写要点：**
```java
public class GenericValueFactory {
    
    /**
     * 创建空实体
     */
    public static GenericValue create(String entityName, Delegator delegator);
    
    /**
     * 创建并设置值
     */
    public static GenericValue create(String entityName, Delegator delegator, Map<String, Object> fields);
    
    /**
     * 从ResultSet创建（查询时使用）
     */
    public static GenericValue fromResultSet(String entityName, Delegator delegator, ResultSet rs);
}
```

**自测验收：**
- [ ] 可创建指定实体类型的GenericValue
- [ ] 自动绑定ModelEntity

**依赖：** T3.1

---

### T3.3 FieldTypeMapping字段类型映射
**状态：** ⬜ 待开始

**详细设计：**
实现语义类型到数据库类型的双向映射，以及Java类型与语义类型的映射。

**代码编写要点：**
```java
public class FieldTypeMapping {
    
    // 语义类型 -> MySQL DDL类型
    public String toSqlType(String semanticType, Integer length);
    
    // 语义类型 -> JDBC类型 (用于PreparedStatement)
    public int toJdbcType(String semanticType);
    
    // JDBC类型 -> Java类型
    public Class<?> toJavaClass(int jdbcType);
    
    // ResultSet值获取
    public Object getValue(ResultSet rs, int columnIndex, String semanticType) throws SQLException;
}
```

**自测验收：**
- [ ] 全量语义类型映射正确
- [ ] DDL生成正确
- [ ] JDBC参数设置正确

**依赖：** T2.4

---

### T3.4 Delegator接口定义
**状态：** ⬜ 待开始

**详细设计：**
`Delegator` 是整个引擎的核心接口，定义所有数据库操作方法。借鉴OFBiz接口设计。

**代码编写要点：**
```java
public interface Delegator {
    
    // ================== 获取基础信息 ==================
    
    String getDelegatorName();
    ModelReader getModelReader();
    DataSource getDataSource();
    
    // ================== 实体创建 ==================
    
    GenericValue makeValue(String entityName);
    GenericValue makeValue(String entityName, Map<String, Object> fields);
    
    // ================== 主键查询 ==================
    
    GenericValue findOne(String entityName, Object... pkValues);
    Optional<GenericValue> findOptional(String entityName, Object... pkValues);
    
    // ================== 列表查询 ==================
    
    List<GenericValue> findList(String entityName, EntityCondition condition);
    List<GenericValue> findList(String entityName, EntityCondition condition, 
                                  List<String> order, int limit);
    
    // ================== 分页查询 ==================
    
    EntityFindOptions find(String entityName);
    
    // ================== 写操作 ==================
    
    <T> T create(GenericValue value) throws EntityException;
    <T> T store(GenericValue value) throws EntityException;
    int remove(GenericValue value) throws EntityException;
    int removeByCondition(String entityName, EntityCondition condition);
    
    // ================== 批量操作 ==================
    
    void storeAll(List<GenericValue> values) throws EntityException;
    void removeAll(List<GenericValue> values) throws EntityException;
    
    // ================== 事务相关 ==================
    
    void runInTransaction(Runnable runnable);
    <T> T runInTransaction(Supplier<T> supplier);
}
```

**自测验收：**
- [ ] 接口方法定义完整
- [ ] 方法命名清晰
- [ ] 异常声明合理

**依赖：** T3.2

---

### T3.5 ConnectionFactory连接工厂
**状态：** ⬜ 待开始

**详细设计：**
封装数据源连接获取、释放逻辑，适配SpringBoot4数据源规范。

**代码编写要点：**
```java
public class ConnectionFactory {
    private final DataSource dataSource;
    
    public Connection getConnection() throws SQLException;
    public void closeConnection(Connection conn);
    public void closeQuietly(Connection conn, Statement stmt, ResultSet rs);
    
    // 获取数据库元数据
    public DatabaseMetaData getDatabaseMetaData() throws SQLException;
}
```

**自测验收：**
- [ ] 可正确获取数据库连接
- [ ] 连接可正确释放

**依赖：** T3.4

---

### T3.6 EntityCondition条件构造器设计
**状态：** ⬜ 待开始

**详细设计：**
实现类OFBiz风格的查询条件构造器，支持链式调用构建复杂条件。

**代码编写要点：**
```java
public class EntityCondition {
    private List<EntityCondition> conditions = new ArrayList<>();
    private EntityOperator operator;
    private String fieldName;
    private Object fieldValue;
    
    // ================== 静态工厂方法 ==================
    
    public static EntityCondition make(String fieldName, EntityOperator operator, Object value);
    
    // ================== 常用条件构造 ==================
    
    public static EntityCondition eq(String fieldName, Object value);      // 等于
    public static EntityCondition ne(String fieldName, Object, Object);         // 不等于
    public static EntityCondition like(String fieldName, String value);     // 模糊
    public static EntityCondition notLike(String, String);                  // 不匹配
    public static EntityCondition in(String fieldName, Collection<?>);     // IN
    
    // ================== 比较条件 ==================
    
    public static EntityCondition gt(String, Comparable);    // 大于
    public static EntityCondition ge(String, Comparable);    // 大于等于
    public static EntityCondition lt(String, Comparable);    // 小于
    public static EntityCondition le(String, Comparable);    // 小于等于
    
    // ================== 非空条件 ==================
    
    public static EntityCondition isNull(String fieldName);
    public static EntityCondition isNotNull(String fieldName);
    
    // ================== 范围条件 ==================
    
    public static EntityCondition between(String fieldName, Object low, Object high);
    
    // ================== 逻辑组合 ==================
    
    public static EntityCondition and(EntityCondition... conditions);
    public static EntityCondition or(EntityCondition... conditions);
    
    // ================== SQL生成 ==================
    
    public String buildWhereClause();
    public List<Object> buildParameters();
}
```

**自测验收：**
- [ ] 所有比较条件可正确构造
- [ ] AND/OR嵌套正确生成SQL
- [ ] 参数绑定正确

**依赖：** T3.5

---

### T3.7 EntityOperator运算符枚举
**状态：** ⬜ 待开始

**详细设计：**
定义条件运算符枚举，封装SQL操作符及其渲染逻辑。

**代码编写要点：**
```java
public enum EntityOperator {
    EQUALS("="),
    NOT_EQUALS("<>"),
    LIKE("LIKE"),
    NOT_LIKE("NOT LIKE"),
    IN("IN"),
    NOT_IN("NOT IN"),
    GREATER_THAN(">"),
    GREATER_THAN_EQUAL_TO(">="),
    LESS_THAN("<"),
    LESS_THAN_EQUAL_TO("<="),
    BETWEEN("BETWEEN"),
    IS_NULL("IS NULL"),
    IS_NOT_NULL("IS NOT NULL"),
    AND("AND"),
    OR("OR");
    
    private final String code;
    
    public String getCode();
    public boolean isComparison();
    public boolean isLogical();
}
```

**自测验收：**
- [ ] 所有运算符定义完整
- [ ] 类型判断方法正确

**依赖：** T3.6

---

### T3.8 QueryBuilder查询构建器
**状态：** ⬜ 待开始

**详细设计：**
封装SELECT查询语句构建逻辑，支持字段选择、JOIN、排序、分页。

**代码编写要点：**
```java
public class QueryBuilder {
    private ModelEntity entity;
    private List<String> selectFields;
    private EntityCondition condition;
    private List<String> orderBy;
    private Integer limit;
    private Integer offset;
    
    public QueryBuilder from(ModelEntity entity);
    public QueryBuilder select(String... fields);
    public QueryBuilder where(EntityCondition condition);
    public QueryBuilder orderBy(String fieldName);
    public QueryBuilder orderBy(String fieldName, boolean desc);
    public QueryBuilder limit(int limit);
    public QueryBuilder offset(int offset);
    
    public String buildSql();
    public List<Object> getParameters();
}
```

**自测验收：**
- [ ] 可构建完整SELECT语句
- [ ] WHERE条件正确
- [ ] ORDER BY语法正确
- [ ] 分页参数正确

**依赖：** T3.6, T3.7

---

### T3.9 InsertBuilder插入语句构建器
**状态：** ⬜ 待开始

**详细设计：**
封装INSERT语句生成逻辑，支持单条插入和批量插入。

**代码编写要点：**
```java
public class InsertBuilder {
    private ModelEntity entity;
    
    public String buildInsertSql(Collection<String> fields, boolean includeGenerated);
    public String buildBatchInsertSql(int batchSize);
    
    // 处理自增主键
    public boolean useAutoGeneratedValue();
}
```

**自测验收：**
- [ ] INSERT语句正确生成
- [ ] 批量插入语法正确
- [ ] 字段处理正确

**依赖：** T3.3

---

### T3.10 UpdateBuilder更新构建器
**状态：** ⬜ 待开始

**详细设计：**
封装UPDATE语句生成，支持主键更新和条件更新。

**代码编写要点：**
```java
public class UpdateBuilder {
    private ModelEntity entity;
    
    public String buildUpdateByPkSql(GenericValue gv);
    public String buildUpdateByConditionSql(GenericValue gv, EntityCondition condition);
    
    // 生成SET子句
    private String buildSetClause(Map<String, Object> fields, List<Object> params);
}
```

**自测验收：**
- [ ] 主键更新正确
- [ ] 条件更新语法正确

**依赖：** T3.9

---

### T3.11 DeleteBuilder删除构建器
**状态：** ⬜ 待开始

**详细设计：**
封装DELETE语句生成，支持主键删除和条件删除。

**代码编写要点：**
```java
public class DeleteBuilder {
    private ModelEntity entity;
    private TableResolver tableResolver;
    
    public String buildDeleteByPkSql(Object pkValue);
    public String buildDeleteByConditionSql(EntityCondition condition);
    
    public List<Object> buildConditions(EntityCondition condition);
}
```

**自测验收：**
- [ ] 主键删除SQL正确
- [ ] 条件删除SQL正确
- [ ] 参数顺序正确

**依赖：** T3.10

---

### T3.12 DefaultDelegator核心实现（基础）
**状态：** ⬜ 待开始

**详细设计：**
实现Delegator接口的核心类，完成基础的连接获取、实体创建、find方法。

**代码编写要点：**
```java
public class DefaultDelegator implements Delegator {
    private final String delegatorName;
    private final ModelReader modelReader;
    private final ConnectionFactory connectionFactory;
    
    @Override
    public GenericValue makeValue(String entityName) {
        ModelEntity model = modelReader.getModelEntity(entityName);
        return GenericValueFactory.create(entityName, this, model);
    }
    
    @Override
    public Optional<GenericValue> findOptional(String entityName, Object... pkValues) {
        // 完整实现主键查询
    }
}
```

**自测验收：**
- [ ] makeValue创建实体正确绑定ModelEntity
- [ ] findOptional查询正确

**依赖：** T3.11

---

### T3.13 DefaultDelegator增加写入方法
**状态：** ⬜ 待开始

**详细设计：**
在DefaultDelegator中实现create/store/remove方法。

**代码编写要点：**
- create()方法：插入实体（包含主键生成）
- store()方法：更新实体
- remove()方法：删除实体

**自测验收：**
- [ ] 单元测试通过
- [ ] JDBC操作正确

**依赖：** T3.12

---

### T3.14 DefaultDelegator条件查询
**状态：** in

**详细设计：**
实现findList和find分页查询，支持EntityCondition条件。

**代码编写要点：**
- findList实现条件查询
- 支持排序
- 支持limit限制
- findList分页查询

**自测验收：**
- [ ] 条件查询正确
- [ ] 分页查询正确
- [ ] 排序功能正确

**依赖：** T3.13

---

## 阶段四：CRUD功能实现

### T4.1 实体数据模型XML定义
**状态：** ⬜ 待开始

**详细设计：**
创建测试用实体定义XML，包含简单实体和复杂索引实体。

**代码编写要点：**
```xml
<!-- 简单测试实体 -->
<entity entity-name="TestUser" table-name="test_user">
    <field name="userId" type="id-ne" is-pk="true"/>
    <field name="username" type="name" not-null="true"/>
    <field name="email" type="description" not-null="true"/>
    <field name="password" type="description" not-null="true"/>
    <field name="status" type="indicator" default="A"/>
    <field name="createTime" type="date-time"/>
    <index name="user_Email" unique="true">
        <index-field name="email"/>
    </index>
</entity>
```

**自测验收：**
- [ ] XML文件放置于demo/resources
- [ ] 可被正常解析

**依赖：** T3.14

---

### T4.2 单元测试框架搭建
**状态：** ⬜ 待开始

**详细设计：**
配置JUnit5测试框架，支持SpringBoot Test和内存数据库。

**代码编写要点：**
- 引入JUnit 5依赖
- 使用SpringBootTest集成测试
- 编写基础测试类

**自测验收：**
- [ ] 测试可运行
- [ ] 测试报告正确

**依赖：** T4.1

---

### T4.3 Create操作测试验证
**状态：** ⬜ 待开始

**详细设计：**
编写单元测试验证create操作正确性。

**自测验收：**
- [ ] 可创建实体并插入数据库
- [ ] 主键生成正确
- [ ] 校验生效

**依赖：** T4.2

---

### T4.4 Read操作测试验证
**状态：** ⬜ 待开始

**详细设计：**
验证主键查询、条件查询、列表查询、分页查询。

**自测验收：**
- [ ] findOne正确
- [ ] findList正确
- [ ] 分页正确

**依赖：** T4.3

---

### T4.5 Update操作测试验证
**状态：** ⬜ 待开始

**详细设计：**
验证store方法和条件更新功能。

**自测验收：**
- [ ] 主键更新正确
- [ ] 条件更新正确

**依赖：** T4.4

---

### T4.6 Delete操作测试验证
**状态：** ⬜ 待开始

**详细设计：**
验证remove方法和条件删除功能。

**自测验收：**
- [ ] 主键删除正确
- [ ] 条件删除正确

**依赖：** T4.5

---

### T4.7 CRUD集成测试
**状态：** ⬜ 待开始

**详细设计：**
完整CRUD流程测试。

**自测验证：**
- [ ] 创建->查询->更新->删除完整流程
- [ ] 数据一致性验证

**依赖：** T4.6

---

## 阶段五：企业级特性

### T5.1 DDLBuilder建表语句生成
**状态：** ⬜ 待开始

**详细设计：**
实现自动生成CREATE TABLE语句功能。

**代码编写要点：**
```java
public class DdlBuilder {
    public String buildCreateTableSql(ModelEntity entity);
    public String buildAddColumnSql(ModelEntity entity, String columnName);
    public String buildCreateIndexSql(ModelEntity entity, ModelIndex index);
}
```

**自测验收：**
- [ ] 建表语句生成正确
- [ ] 添加字段语句正确
- [ ] 创建索引语句正确

**依赖：** T4.7

---

### T5.2 SchemaComparator模式对比
**状态：** ⬜ 待开始

**详细设计：**
实现数据库表结构与元数据模型的对比，判断是否需要创建表/字段/索引。

**自测验收：**
- [ ] 可判断表是否存在
- [ ] 可判断字段是否缺失
- [ ] 可判断索引是否缺失

**依赖：** T5.1

---

### T5.3 AutoDdl自动建表处理器
**状态：** ⬜ 待开始

**详细设计：**
启动时自动检查并创建表结构。

**自测验收：**
- [ ] 启动项目自动建表
- [ ] 增量添加字段
- [ ] 增量添加索引

**依赖：** T5.2

---

### T5.4 EntityCache一级缓存实现
**状态：** ⬜ 待开始

**详细设计：**
实现线程级一级缓存，存储查询结果。

**自测验收：**
- [ ] 同线程重复查询命中缓存
- [ ] 缓存正确性验证
- [ ] 提供缓存统计

**依赖：** T5.3

---

### T5.5 TransactionManager事务管理
**状态：** ⬜ 待开始

**详细设计：**
整合SpringBoot事务支持。

**自测验收：**
- [ ] @Transactional注解生效
- [ ] 事务回滚正确
- [ ] 异常处理正确

**依赖：** T5.4

---

### T5.6 MiniOfbizProperties配置属性类
**状态：** ⬜ 待开始

**详细设计：**
定义yml配置属性。

**自测验收：**
- [ ] 配置正确绑定
- [ ] 默认值正确

**依赖：** T5.5

---

### T5.7 MiniOfbizAutoConfiguration自动配置
**状态：** ⬜ 待开始

**详细设计：**
编写SpringBoot自动配置类，自动注册Bean。

**自测验收：**
- [ ] Bean自动注册
- [ ] 条件装配正确

**依赖：** T5.6

---

### T5.8 spring.factories配置更新
**状态：** ⬜ 待开始

**详细设计：**
编写SpringBoot自动配置（SpringBoot4新规范）。

**自测验收：**
- [ ] Starter引入后自动装配

**依赖：** T5.7

---

## 阶段六：RBAC权限系统

### T6.1 sys_user实体定义
**状态：** ⬜ 待开始

**详细设计：**
定义用户实体XML。

**代码编写要点：**
```xml
<entity entity-name="SysUser" table-name="sys_user">
    <field name="userId" type="id-ne" is-pk="true"/>
    <field name="username" type="name" not-null="true"/>
    <field name="password" type="description" not-null="true"/>
    <field name="realName" type="name"/>
    <field name="email" type="description"/>
    <field name="phone" type="description"/>
    <field name="status" type="indicator" default="A"/>
    <field name="createTime" type="date-time"/>
    <field name="updateTime" type="date-time"/>
    <index name="user_Name" unique="true">
        <index-field name="username"/>
    </index>
</entity>
```

**自测验收：**
- [ ] 表自动创建成功

**依赖：** T5.8

---

### T6.2 sys_role实体定义
**状态：** ⬜ 待开始

**详细设计：**
定义角色实体。

**自测验收：**
- [ ] 角色表自动创建

**依赖：** T6.1

---

### T6.3 sys_user_role关联表定义
**状态：** ⬜ 待开始

**详细设计：**
用户-角色多对多关联表。

**自测验收：**
- [ ] 关联表正确创建
- [ ] 复合索引正确

**依赖：** T6.2

---

### T6.4 sys_menu菜单权限实体
**状态：** ⬜ 待开始

**详细设计：**
菜单/权限实体，支持树形结构。

**自测验收：**
- [ ] 表结构正确

**依赖：** T6.3

---

### T6.5 sys_role_menu关联表
**状态：** ⬜ 待开始

**详细设计：**
角色-菜单权限关联表。

**自测验收：**：
- [ ] 关联表正确

**依赖：** T6.4

---

### T6.6 PasswordEncoder密码加密
**状态：** ⬜ 待开始

**详细设计：**
实现密码加密工具类。

**自测验收：**
- [ ] 加密解密正确
- [ ] 随机盐值

**依赖：** T6.5

---

### T6.7 UserService用户服务
**状态：** ⬜ 待开始

**详细设计：**
用户管理服务，包含增删改查。

**自测验收：**
- [ ] 所有操作正确

**依赖：** T6.6

---

### T6.8 LoginService登录服务
**状态：** ⬜ 待开始

**详细设计：**
用户登录认证服务。

**自测验收：**
- [ ] 登录成功返回Token
- [ ] 登录失败正确提示

**依赖：** T6.7

---

### T6.9 PermissionService权限服务
**状态：** ⬜ 待开始

**详细设计：**
查询用户角色和权限。

**自测验收：**
- [ ] 查询用户权限正确
- [ ] 查询用户角色正确

**依赖：** T6.8

---

### T6.10 @HasPermission权限注解
**状态：** ⬜ 待开始

**详细设计：**
自定义接口权限注解。

**代码编写要点：**
```java
@Target({ElementType.METHOD, ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
@Documented
public @interface HasPermission {
    String[] value() default {};
    Logical logical() default Logical.AND;
}
```

**自测验收：**
- [ ] 注解定义正确

**依赖：** T6.9

---

### T6.11 @HasRole角色注解
**状态：** ⬜ 待开始

**详细设计：**
定义角色控制注解。

**自测验收：**
- [ ] 注解解析正确

**依赖：** T6.10

---

### T6.12 AuthorityInterceptor权限拦截器
**状态：** ⬜ 待开始

**详细设计：**
实现SpringMVC拦截器，校验登录状态和权限。

**自测验收：**
- [ ] 未登录拦截
- [ ] 无权限拦截
- [ ] 有权限放行

**依赖：** T6.11

---

### T6.13权限AOP切面
**状态：** ⬜ 待开始

**详细设计：**
AOP处理@HasPermission和@HasRole注解。

**自测验收：**
- [ ] 方法级权限校验正确

**依赖：** T6.12

---

### T6.14 SessionManager会话管理
**状态：** ⬜ 待开始

**详细设计：**
管理用户登录会话。

**自测验收：**
- [ ] 会话存储正确
- [ ] 会话过期处理

**依赖：** T6.13

---

### T6.15 RBAC集成测试
**状态：** ⬜ 待开始

**详细设计：**
全量权限功能测试。

**自测验收：**
- [ ] 用户-角色-权限完整流程验证
- [ ] 拦截器效果验证

**依赖：** T6.14

---

## 项目统计

| 阶段 | 任务数 | 预计耗时 |
|------|--------|----------|
| 阶段一：项目骨架 | 5 | 1天 |
| 阶段二：元数据 | 8 | 2-3天 |
| 阶段三：引擎核心 | 14 | 3-4天 |
| 阶段四：CRUD | 7 | 2天 |
| 阶段五：企业特性 | 8 | 2-3天 |
| 阶段六：RBAC | 15 | 3-4天 |
| **总计** | **57** | **15天** |

---

## 快速导航

开始新工作时，建议按以下顺序：
1. 确认前置任务已完成
2. 将任务状态更新为 🔄 进行中
3. 完成后更新为 ✅ 已完成

如需暂停某任务，可标记为 ⏸️ 已阻塞并说明原因。
